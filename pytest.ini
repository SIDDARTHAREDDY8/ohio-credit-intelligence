[pytest]
testpaths = tests
markers =
    integration: end-to-end tests that need the live stack (DB, MLflow, Claude). Excluded from CI unit runs.
    eval: adverse-action notice evaluation harness; calls the Claude API. Excluded from CI unit runs.
addopts = -ra
